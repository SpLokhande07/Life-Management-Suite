import 'package:flutter/widgets.dart';

class SocailHubView extends StatelessWidget {
  const SocailHubView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
